[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn main:app --bind 0.0.0.0:$PORT"

[deploy.healthcheck]
path = "/health"
interval = 30
timeout = 10

[variables]
PYTHONUNBUFFERED = "1"
PORT = "8000"
SECRET_KEY = "your-secret-key-change-this-123"
