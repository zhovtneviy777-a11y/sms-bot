[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn main:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"

[variables]
PYTHONUNBUFFERED = "1"
PORT = "8000"
SECRET_KEY = "railway-secret-123"
ADMIN_PASSWORD = "your-admin-password"
